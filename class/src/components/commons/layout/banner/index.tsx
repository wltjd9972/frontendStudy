import styled from "@emotion/styled";

const Banner = styled.div`
  height: 100px;
  background-color: skyblue;
`;

export default function LayoutBanner(): JSX.Element {
  return <Banner>여기는 배너입니다</Banner>;
}
